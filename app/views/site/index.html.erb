<h1>Upcoming Donations</h1>

<br/>
<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
<%= link_to "Sign Up" %>
<%= link_to "Sign In" %>
<h2>Latest Donations</h2>
<table style="width: 100%">
  <thead>
    <tr>
      <th>Donation List</th>
    </tr>
  </thead>

  <tbody>
  <% @donations.each do |donation| %>
    <tr>
      <td><%= link_to donation.title %></td>
    </tr>
  <% end %>
  </tbody>
</table>